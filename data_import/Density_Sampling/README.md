The density sampling function is taken from https://github.com/GGiecold-zz/Density_Sampling .
We can't just "pip install" it as the published version is Python 2 code. We have adapted it to Python 3
and include it directly.
